import { OnChanges, OnDestroy, SimpleChanges } from '@angular/core';
import { PayPalConfig } from '../models/paypal-models';
import { ScriptService } from '../services';
export declare class NgxPaypalComponent implements OnChanges, OnDestroy {
    private scriptService;
    /**
     * Configuration for paypal.
     */
    config?: PayPalConfig;
    /**
    * Name of the global variable where paypal is stored
    */
    private readonly paypalWindowName;
    /**
     * Id of the element where PayPal button will be rendered
     */
    paypalButtonContainerId?: string;
    private readonly ngUnsubscribe;
    constructor(scriptService: ScriptService);
    ngOnChanges(changes: SimpleChanges): void;
    ngOnDestroy(): void;
    private getPayPalSdkUrl;
    private initPayPalScript;
    private getQueryString;
    private generateElementId;
    private initPayPal;
}
